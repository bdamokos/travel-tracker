# Raspberry Pi Connection
PI_HOST=your.pi.ip.address  # Replace with your Pi's IP address
PI_USER=bence             # SSH user for your Pi
DEPLOY_PATH=/home/bence/travel-tracker  # Where to deploy on Pi

# Registry Configuration
# Used for tagging/pushing from your dev machine to the Pi
REGISTRY_HOST=your.registry.host  # Replace with your Pi's IP (LAN/Tailscale)

# Optional: Override for pulling on the Pi
# If your registry runs on the same Pi, use loopback to avoid TLS requirements
# (Docker allows plain HTTP for localhost by default)
# REGISTRY_PULL_HOST=127.0.0.1

# Port Configuration (adjust to avoid conflicts with your other services)
ADMIN_PORT=3028
EMBED_PORT=3029

# Data Storage Path on Pi
DATA_PATH=~/travel-tracker/data

# Domain Configuration (replace these examples with your actual domains)
ADMIN_DOMAIN=https://tt-admin.yourdomain.com
EMBED_DOMAIN=https://travel-tracker.yourdomain.com

# GitHub Repository URL (optional, for footer link)
NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/bdamokos/travel-tracker

# Optional: Additional configuration
NODE_ENV=production 
